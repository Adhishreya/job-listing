import { SalaryPipePipe } from './salary-pipe.pipe';

describe('SalaryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
